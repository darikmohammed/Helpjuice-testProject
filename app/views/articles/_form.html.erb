<%= form_with(model: article) do |form| %>
  <% if article.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(article.errors.count, "error") %> prohibited this article from being saved:</h2>

      <ul>
        <% article.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class='flex-col gap'>  
      <%= form.text_field :title, class: 'text-field', placeholder: 'Article Title' %>

      <%= form.text_field :publisher, class: 'text-field', placeholder: 'Article Publisher' %>

      <%= form.number_field :pbulished_year, class: 'text-field', placeholder: 'Published Year' %>

      <%= form.submit 'Create Article', class: 'btn' %>
  </div>

<% end %>
